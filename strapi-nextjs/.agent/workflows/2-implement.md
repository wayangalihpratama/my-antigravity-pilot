---
description: Implement phase - Red-Green-Refactor with Vitest
---

1. Create a failing test in `backend/tests/` or `frontend/tests/`.
2. Run tests: `./dc.sh exec backend npm test` or `./dc.sh exec frontend npm test`.
3. Implement minimal code change to pass the test.
4. Refactor and verify.

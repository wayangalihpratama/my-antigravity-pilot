---
description: Ship phase - Commit changes using Conventional Commits
---

1. Run `./dc.sh exec app flake8 .` to check linting.
2. Stage changes: `git add .`.
3. Commit with conventional commit message (e.g., `feat(ai-stack): add new AI components`).
4. Push to remote.

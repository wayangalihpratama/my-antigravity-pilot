---
description: Implement phase - Red-Green-Refactor with Class-Based Python testing
---

1. Create/update Pydantic schemas in `app/schemas/`.
2. Write a failing test in `tests/`.
3. Implement the minimal logic in `app/`.
4. Run tests: `./dc.sh tests`.
5. Refactor and repeat.

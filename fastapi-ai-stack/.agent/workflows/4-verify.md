---
description: Verify phase - run full validation suite
---

1. Run full test suite: `./dc.sh tests`.
2. Verify API documentation at `http://localhost:8000/docs`.
3. Check `guardrails` skill checklist.
4. Create a `walkthrough.md` with proof of work.
